states:
{% for version in __pillar__.get('L0C', {}).get('versions', ['default_version']) %}
  - state_C.{{ version }}
{% endfor %}

pillars:
  same_plaintext_pillar: from_L0C
  same_list_pillar:
    - ^
    - L0C-1
    - L0C-2
  expansion:
    plaintext: ${same_plaintext_pillar}
    list: ${same_list_pillar}
